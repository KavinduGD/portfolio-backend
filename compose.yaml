name: portfolio

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
    volumes:
      - certificate_data:/app/certificate

volumes:
  certificate_data:
